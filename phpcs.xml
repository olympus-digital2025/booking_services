<?xml version="1.0"?>
<ruleset name="Custom Plugin">
    <description>WordPress Coding Standards for Custom Plugin</description>

    <!-- Scan plugin files only (exclude vendor) -->
    <file>.</file>

    <!-- WordPress PHP standards -->
    <rule ref="WordPress-Core"/>
    <rule ref="WordPress-Extra"/>
    <rule ref="WordPress-Docs"/>

    <!-- Indent: 2 spaces (override WordPress default of tabs) -->
    <arg name="tab-width" value="2"/>
    <rule ref="Generic.WhiteSpace.DisallowSpaceIndent">
        <severity>0</severity>
    </rule>
    <rule ref="Generic.WhiteSpace.ScopeIndent">
        <properties>
            <property name="indent" value="2"/>
            <property name="tabIndent" value="false"/>
        </properties>
    </rule>
    <rule ref="PEAR.Functions.FunctionCallSignature">
        <properties>
            <property name="indent" value="2"/>
        </properties>
    </rule>
    <rule ref="Universal.WhiteSpace.PrecisionAlignment">
        <severity>0</severity>
    </rule>

    <!-- #22: Cyclomatic complexity ≤ 10 -->
    <rule ref="Generic.Metrics.CyclomaticComplexity">
        <properties>
            <property name="complexity" value="10"/>
            <property name="absoluteComplexity" value="20"/>
        </properties>
    </rule>

    <!-- #23: Max nesting depth 3 -->
    <rule ref="Generic.Metrics.NestingLevel">
        <properties>
            <property name="nestingLevel" value="3"/>
            <property name="absoluteNestingLevel" value="5"/>
        </properties>
    </rule>

    <!-- #64: No commented-out code -->
    <rule ref="Squiz.PHP.CommentedOutCode">
        <properties>
            <property name="maxPercentage" value="35"/>
        </properties>
    </rule>

    <!-- #65: No TODO/FIXME comments -->
    <rule ref="Generic.Commenting.Todo"/>
    <rule ref="Generic.Commenting.Fixme"/>

    <!-- Ignore vendor, build, scripts, and assets (e.g. lint runner) -->
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>build/*</exclude-pattern>
    <exclude-pattern>scripts/*</exclude-pattern>
    <exclude-pattern>assets/*</exclude-pattern>

    <!-- Custom capabilities for this plugin -->
    <config name="custom_capabilities" value="manage_services,manage_service_bookings,manage_service_workers,view_all_bookings,book_services,manage_own_bookings,leave_service_reviews,edit_services"/>

    <!-- CLI -->
    <arg value="p"/>
    <arg name="colors"/>
</ruleset>